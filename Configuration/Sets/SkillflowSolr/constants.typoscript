plugin.tx_solr.index.queue.skills {
  # nr_llm stores skills at the root (pid=0).
  additionalPageIds = 0

  # Page containing the skillflow_skilldetail content element.
  # Solr uses it to build result links.
  detailPageId =
}
