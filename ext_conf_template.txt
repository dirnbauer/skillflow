# cat=runner/enable/100; type=options[Auto: prefer nr_llm, else Anthropic API=api,Anthropic API (env key)=anthropic,Claude Code CLI=cli]; label=Skill runner: "api" prefers the LLM connection configured in nr_llm (the "LLM" backend module) and falls back to the Anthropic Messages API env key; "anthropic" forces the env-key API; "cli" executes the local Claude Code CLI
runner = api

# cat=runner/enable/110; type=string; label=Model: Model id used by the API runner
model = claude-sonnet-4-6

# cat=runner/enable/120; type=string; label=API key environment variable: Name of the env var that holds the Anthropic API key. The key itself is never stored in the database.
apiKeyEnvVar = ANTHROPIC_API_KEY

# cat=runner/enable/130; type=string; label=Claude CLI binary: Path or name of the claude binary (cli runner)
claudeBinary = claude

# cat=runner/enable/140; type=int+; label=Max output tokens (api runner)
maxTokens = 2048

# cat=runner/enable/150; type=string; label=MCP servers JSON (api runner, optional): JSON array for the Anthropic MCP connector (remote MCP servers). Leave empty to disable.
mcpServersJson =

# cat=runner/enable/155; type=string; label=MCP config JSON (claude-cli runner, optional): Claude Code .mcp.json content, e.g. {"mcpServers":{"typo3":{"command":"vendor/bin/typo3","args":["mcp:server"]}}} to let skills call the abilities registry as MCP tools. Whitelist the tools per skill via allowed-tools. Leave empty to disable.
mcpConfigJson =

# cat=import/enable/100; type=string; label=Skills folder: Folder with skill directories (each containing a SKILL.md), relative to the project root
skillsFolder = skills

# cat=import/enable/110; type=int+; label=Skills storage page: pid of the SysFolder that imported skills are stored in (0 = global/rootLevel). Set this to a SysFolder inside your Solr site so skills get indexed.
storagePid = 0

# cat=runner/enable/160; type=string; label=Default engine: Execution engine used when neither the run nor the skill (frontmatter key "engine") requests one. "classic" = the built-in single-shot chain above; other values match engines registered by other extensions (e.g. "flue").
defaultEngine = classic

# cat=runner/enable/170; type=boolean; label=Engine fallback: When the selected context-aware engine is unavailable, fall back to the classic chain instead of blocking the run.
engineFallback = 1

# cat=security/enable/100; type=boolean; label=Require local environment: Only allow skill execution when TYPO3 runs in Development context inside a DDEV project (IS_DDEV_PROJECT=true). Do NOT disable this on shared or production systems.
requireLocalEnvironment = 1

# cat=security/enable/110; type=boolean; label=SkillSpector scan: Run NVIDIA SkillSpector (github.com/NVIDIA/skillspector) on every imported and re-checked skill when the binary is available. A DO_NOT_INSTALL verdict quarantines the skill like any danger-level finding. When the binary is missing the built-in checks still run and the report notes the missing scan.
skillspectorEnabled = 1

# cat=security/enable/120; type=string; label=SkillSpector binary: Path or name of the skillspector binary. Install with "uv tool install git+https://github.com/NVIDIA/skillspector.git".
skillspectorBinary = skillspector

# cat=security/enable/130; type=boolean; label=SkillSpector LLM analysis: Also run SkillSpector's LLM-assisted semantic analysis (filters false positives). Credentials are taken from the nr_llm "LLM" module's default connection (provider + vault key) when installed, else from ambient SKILLSPECTOR_* env vars; if neither resolves the scan stays static. SENDS SKILL CONTENT to that provider (e.g. OpenAI). Off = static analysis only (--no-llm), fully local.
skillspectorUseLlm = 0

# cat=security/enable/140; type=int+; label=SkillSpector timeout: Seconds before a SkillSpector scan is aborted (the report then notes the failure; the import itself never fails).
skillspectorTimeout = 120
